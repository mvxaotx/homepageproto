
  # Homepage Prototype

  This is a code bundle for Homepage Prototype. The original project is available at https://www.figma.com/design/djMXFPhJJ6a7abi3tbTKFu/Homepage-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  